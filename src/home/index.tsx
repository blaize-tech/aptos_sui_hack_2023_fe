import { Box, Container } from '@chakra-ui/react';
import { HtmlMeta } from '@look/components';

const Home = () => {
  return (
    <Box>
      <HtmlMeta />
      <Box as="section">
        Home page layout
      </Box>
    </Box>
  );
};

export default Home;
